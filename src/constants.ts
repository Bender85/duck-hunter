export const COLORS = {
    BLUE: "#9fa2cb",
    RED: "#cb7387",
    BEIGE: "#ffccc5",
};

export const GAME_WIDTH = 256;
export const GAME_HEIGHT = 224;

export const BULLET_SPEED = 500;
export const DUCK_SPEED = 100;
export const DUCKS_PER_HUNT = 3;

export const PREY_SPAWN_RATE = 1.5; // in seconds
export const PREY_SPAWN_RATE_INCREASE = 0.1; // in seconds per round

export const MAX_BULLETS = 3;

export const ROUND_DURATION = 30; // in seconds
export const ROUND_START_DELAY = 2; // in seconds

export const GAME_OVER_DELAY = 3; // in seconds

export const fontConfig = {
    font: "nes",
    size: 8,
}